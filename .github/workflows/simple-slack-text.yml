name: Send Slack notification

on:
  push:
    branches: [main]

jobs:
  send_notification:
    runs-on: ubuntu-latest
    steps:
      - name: Send Slack notification
        run: curl -X POST -H 'Content-type:application/json' --data '{"text":"Hello from GitHub Actions"}' $SLACK_WEBHOOK_URL
